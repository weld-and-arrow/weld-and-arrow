name: Notify site

on:
  push:
    branches: [main]

jobs:
  notify:
    runs-on: ubuntu-latest
    env:
      SITE_APP_ID: ${{ secrets.SITE_APP_ID }}
      SITE_APP_PRIVATE_KEY: ${{ secrets.SITE_APP_PRIVATE_KEY }}
    steps:
      - name: Mint app token
        if: ${{ env.SITE_APP_ID != '' && env.SITE_APP_PRIVATE_KEY != '' }}
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ env.SITE_APP_ID }}
          private-key: ${{ env.SITE_APP_PRIVATE_KEY }}
          repositories: weld-and-arrow-site
      - name: Dispatch site rebuild
        if: ${{ env.SITE_APP_ID != '' && env.SITE_APP_PRIVATE_KEY != '' }}
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          SITE_REPO: ${{ github.repository_owner }}/weld-and-arrow-site
          SOURCE_SHA: ${{ github.sha }}
        run: |
          gh api "repos/${SITE_REPO}/dispatches" \
            --method POST \
            --field event_type=source-push \
            --raw-field client_payload="{\"sha\":\"${SOURCE_SHA}\"}"

      - name: Skip dispatch without token
        if: ${{ env.SITE_APP_ID == '' || env.SITE_APP_PRIVATE_KEY == '' }}
        run: echo "::warning::SITE_APP_ID or SITE_APP_PRIVATE_KEY is not configured; site redeploy dispatch was skipped."
